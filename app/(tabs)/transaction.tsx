import React from "react";
import { Text, View } from "react-native";

const Transaction = () => {
  return (
    <View className="flex-1 items-center justify-center ">
      <Text className="text-accent text-2xl font-bold">transaction</Text>
    </View>
  );
};

export default Transaction;
